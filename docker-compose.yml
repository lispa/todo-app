version: '3.8'

services:
  db:
    image: postgres:15
    container_name: todo-app-db-1
    environment:
      POSTGRES_USER: admindb
      POSTGRES_PASSWORD: adminpassword
      POSTGRES_DB: todo_db
    ports:
      - "5432:5432"

  app:
    build: .
    ports:
      - "8080:8080"
    depends_on:
      - db
    environment:
      DB_HOST: db
      DB_PORT: 5432
      DB_USER: admindb
      DB_PASSWORD: adminpassword
      DB_NAME: todo_db

  nginx:
    image: nginx:latest
    container_name: todo-app-nginx
    ports:
      - "80:80"
    volumes:
      - ./web:/usr/share/nginx/html:ro  # Монтируем папку с фронтендом
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro # Монтируем конфиг
    depends_on:
      - app